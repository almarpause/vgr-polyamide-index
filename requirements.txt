# The Zara Polyamide Index build uses only the Python standard library
# (urllib for live FX, json, sqlite3). No third-party packages are required —
# this file exists so the GitHub Action's `pip install -r requirements.txt`
# step is a no-op that always succeeds. Add pins here if that ever changes.
